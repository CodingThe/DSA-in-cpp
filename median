#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
int arr[n];
for(int i=0;i<n;i++){
    cin>>arr[i];
}
int sum=0;
sort(arr,arr+n);
if(n%2==0){
    cout<<arr[n/2]<<endl;
}
else{
   cout<<(arr[n/2]+arr[n-1])/2<<endl; 
}
    return 0;
}
